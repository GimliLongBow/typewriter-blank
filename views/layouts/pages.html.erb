<!DOCTYPE html>
<html lang="en">
<head>
	<!-- LAYOUT pages.html.erb -->
	<%= page_title(@page, 'Ronningen Design', ',') %>
	<%= meta_tags(:html5 => true) %>
	<%= description(@page) %>
	<%= key_phrases(@page) %>

	<!-- ANDREW - can we set up something like we have for the seo elements for :og tags? it can be the same info coming in as for seo for now, btu in the future we may want to be able edit it independently. We can talk... - Steve -->
	<meta property="og:title" content="{{title}}" />
	<meta property="og:description" content="{{description}}" />
	<meta property="og:image" content="/themes/typewriter-blank/images/og-logo.jpg" />

	<%= render :partial => '/shared/stylesMeta' %>
</head>
<body id="<%= @page.friendly_id %>" class="<%= @current_action %> <%= @current_controller %>">
	<div id="wrapper">
		<%= render :partial => '/shared/header' %>

		<section class="global">
			<%= yield %>
		</section>

		<%= render :partial => '/shared/aside' %>

		<%= render :partial => '/shared/footer' %>

		<%= render :partial => '/shared/scripts' %>
	</div>
</body>
</html>